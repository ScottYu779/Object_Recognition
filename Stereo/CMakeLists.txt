set(ProjectName stereo)
project(${ProjectName})
set(Project_SRC Sample.cpp)
add_executable(${ProjectName} ${Project_SRC}) 
find_package(OpenCV REQUIRED)
target_link_libraries(${ProjectName} ${DUO_LIBRARY} ${DENSE3D_LIBRARY} ${OpenCV_LIBS})
